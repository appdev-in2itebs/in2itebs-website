# SDD ledger — plan: docs/superpowers/plans/2026-09-08-website-remediation-phase-2.md
Spec: audit artifact https://claude.ai/code/artifact/37eae16e-7085-4d74-b80c-066ac6a16917 (findings C1–C3, H1–H5, M1–M12, L1–L9); supporting docs/WEBSITE_AUDIT_2026-09-07.md.
Started: 2026-09-08 15:50 IST. Controller: Fable 5.1. Implementers: opus. Reviewers: sonnet (mechanical) / opus (multi-file, logic). Final review: fable.

## Pre-flight rulings
- Ruling: no git worktree; work in the main tree on branch remediation/2026-09-08 — the preview server on 3107, node_modules and the other active session all live in this tree — cost if wrong: another session's uncommitted edits can collide with a task; mitigated by re-read-before-edit and one commit per task.
- Ruling: controller ran a bare `git init -b main` as bootstrap because the skill's workspace/brief scripts need a repository; Task 0.1's implementer does the ignore entries, baseline commit and branch — cost if wrong: none (no commit made by the controller).
- Ruling: Task 0.1 is reviewed from a repository-hygiene report (tracked-file counts, check-ignore, secrets grep) instead of a diff package; the baseline commit's diff is the whole repo and carries no reviewable change — cost if wrong: a stray generated file in the baseline, fixable in a later commit.
- Ruling: batched dispatches — {2.3, 2.4}, {3.1, 3.4}, {4.1, 4.2, 4.6} — each pair/triple is small same-shape work on adjacent files; reviewed as one unit — cost if wrong: a review that rejects one part rejects the batch.
- Ruling: models — implementers opus (user instruction), reviewers sonnet for single-file/mechanical diffs and opus for multi-file or logic diffs, final whole-branch review fable — cost if wrong: a cheaper reviewer misses a subtle defect; the final review is the net.

## Pre-flight conflict scan
| Tasks | Shared file / interface | Produces vs consumes | Finding |
|---|---|---|---|
| 0.2 ↔ 1.2 | playwright webServer env | 0.2 passes SITE_ENV=preview; 1.2 makes next start refuse without it | consistent |
| 1.1 ↔ 2.4 | lib/utils.ts SITE_URL | 1.1 redefines with env override; 2.4 imports it in sitemap/json-ld | consistent (1.1 first) |
| 1.2 ↔ 1.5 ↔ 2.2 | next.config.mjs | 1.2 adds siteEnv + guard; 1.5 replaces headers() using siteEnv; 2.2 adds rewrites + goneSources import | consistent, sequential |
| 1.2 ↔ 2.1 ↔ 2.3 | scripts/check-build.mjs | 2.1 appends prerender assertion before final block; 2.3 adds production robots assertions inside the production branch | consistent |
| 1.1 ↔ 1.3 | lib/lead-service.ts, foundations.test.ts | 1.1 edits origin check; 1.3 edits limiter + inserts email limit; 1.3 replaces old rate-limit test | different regions, consistent |
| 1.4 → 2.1 | contact page process.env | 1.4 removes build-time env read; 2.1 needs it gone for static render | dependency honoured by order |
| 1.5 → 2.1 | CSP 'unsafe-inline' | 2.1 inline theme script needs it | dependency honoured by order |
| 2.1 ↔ 4.3 | tests/browser/site.spec.ts, interactions.spec.ts, transcript.spec.ts | 2.1 rewrites no-JS test; 4.3 changes heading levels in three specs | sequential edits of same files; 4.3 must re-read |
| 2.3 → 2.4 | app/robots.ts | 2.3 rewrites robots to import SITE_URL; 2.4 test asserts robots contains SITE_URL and no BASE | consistent (2.3 first) |
| 2.4 ↔ 3.3 ↔ 3.5 | sitemap.ts, json-ld.tsx | 2.4 swaps BASE→SITE_URL; 3.3 rewrites sitemap return using SITE_URL; 3.5 rewrites json-ld importing SITE_URL; 2.4 test still true afterwards | consistent |
| 3.1 → 3.2/3.3/3.5 | tests/metadata.test.ts | 3.1 creates; later tasks append; `pages()` helper defined in 3.2 and reused by 3.5 | consistent (3.2 before 3.5) |
| 3.1 ↔ 3.4 | pageMetadata title union | 3.1 adds {absolute}; 3.4 uses string | consistent |
| 3.2 ↔ 3.5 | app/**/page.tsx | 3.2 edits descriptions; 3.5 inserts breadcrumb line | sequential |
| 4.1 ↔ 4.2 ↔ 4.6 ↔ 4.7 | footer.tsx, app/page.tsx | different lines; quoted by content not line number | consistent; batched 4.1+4.2+4.6 |
| 1.4 ↔ 4.7 | demo-form.tsx | 1.4 edits status/button; 4.7 replaces region options | different regions |
| 5.1 test ↔ 2.2/3.5 | unused-module scan | scans components/content only; next.config included as importer of redirects.mjs | consistent |
| 5.2 ↔ 0.2 | package.json check scripts | 0.2 adds check:full calling check; 5.2 prepends format:check to check | consistent |
| every task | self-consistency (tests vs code, files created vs touched) | checked: 1.1 test relies on x-forwarded-host not Host header; 2.1 prerender threshold 60 vs 62 sitemap routes + _not-found; 3.5 regex matches template-literal paths; 4.3 first visible h2 is the slide heading (dialog h2 hidden) | no defects found |

## Progress
- Task 0.1: implementer DONE_WITH_CONCERNS (commit 0a1a094 baseline on main; branch remediation/2026-09-08). Ruling: keep the repo-local git identity "Pralay Mishra <appdev@in2itebs.com>" (the user's own address) — cost if wrong: author line on this branch's commits, amendable before any push. Ruling: system-wide core.autocrlf=true with no .gitattributes is a real regression risk (checkouts would rewrite LF files); Task 0.2's dispatch adds `git config --local core.autocrlf false` and a root `.gitattributes` (`* text=auto eol=lf`) — cost if wrong: a one-time line-ending normalisation commit. Plan defect noted: Task 0.1 Step 4's secrets grep necessarily matches the tracked web/.env.example (placeholders only); accepted.
- Task 0.1: complete (commit 0a1a094 baseline, review clean; minor: repo-local git identity disclosed)
- Task 0.2: BASE 0a1a094; dispatched implementer (opus) with the .gitattributes/autocrlf ruling
- Task 0.2: implementer DONE_WITH_CONCERNS (commits fa01adf .gitattributes, 072ea33 playwright/webServer + check:full). Ruling: `reuseExistingServer:true` stays — it protects the user's running server and CI has no pre-existing server — but locally `check:full` can test a stale 3107 build; carry a runbook note into Task 1.2's docs step ("stop or rebuild the 3107 server before check:full") — cost if wrong: a locally green check:full against old code; CI is the authoritative gate. Deferred minor: SITE_ENV path of webServer untested until a cold start (CI will exercise it).
- Task 0.2: complete (commits 0a1a094..072ea33, review clean; controller verified core.autocrlf=false locally and both trailers present)
- Task 0.3: BASE 072ea33; dispatched implementer (opus)
- Task 0.3: implementer DONE (commit 0afb9b0). Deferred minor: playwright reuseExistingServer is unconditional (convention !process.env.CI); CI steps call check/build/test:browser individually rather than check:full (may drift)
- Task 0.3: complete (commits 072ea33..0afb9b0, review clean; controller verified trailer). Phase 0 gate: `npm run check` green; browser suite unchanged since the 42/42 run at 11:xx (no runtime code touched). Ruling: Phase 0 gate does not re-run build/browser — cost if wrong: none (config-only phase).
- Ruling: the `next start` on 127.0.0.1:3107 was started by the controller at 15:05 for the Cloudflare preview; implementers MAY restart that one process after a build (kill the node/cmd process listening on 3107, then `SITE_ENV=preview npm start` detached) so their browser verification runs against their build; ngrok and cloudflared stay untouched — cost if wrong: a few seconds of preview downtime per restart.
- Task 1.1: BASE 0afb9b0; dispatched implementer (opus)
- Task 1.1: implementer DONE_WITH_CONCERNS after a rate-limit interruption (commit d74e9e5 landed; report written on resume; 12/12 unit tests). Ruling: the `in2it-lead-receipt` cookie derives `Secure` from `new URL(request.url).protocol`, which is `http:` behind the proxy — carry into Task 1.3's dispatch (same file): set Secure when the forwarded proto is https under TRUST_PROXY_HEADERS=1 or when SITE_URL is https — cost if wrong: a 5-minute receipt cookie without the Secure flag. Deferred minor: no test pins the negative trust-gate case or ALLOWED_ORIGINS (reviewer to weigh).
- Task 1.1: review Approved with 1 Important (plan-mandated): allowlist entries not normalised to origins. Ruling: accept the finding over the plan text — normalise forwarded and ALLOWED_ORIGINS entries via `new URL(...).origin` — cost if wrong: none (strictly more permissive matching of equivalent origins). Fix round 1 also takes three small minors: negative trust-gate test, `Set<string>` return annotation, build-time note for NEXT_PUBLIC_SITE_URL in .env.example. Deferred minors: one-directional www handling; leftmost forwarded value trust comment.
- Task 1.1: automated security review flagged the TRUST_PROXY_HEADERS forwarded-origin derivation as a spoofable-field bypass. Ruling: not exploitable here — the check is CSRF protection (no auth), browsers cannot attach X-Forwarded-* cross-site without a preflight the route never grants, non-browser clients already pass by omitting Origin, and the flag is documented as ingress-overwrite-only; keep the design, extend the .env.example warning to X-Forwarded-Host/Proto — cost if wrong: an operator who sets the flag behind a non-overwriting ingress weakens the CSRF filter to the level non-browser clients already have.
- Task 1.1: fix round 1/5 (4 findings + env note addressed per implementer, 0 open pending re-review; commits d74e9e5..25b0ce1)
- Task 1.1: complete (commits 0afb9b0..25b0ce1, review clean after 1 fix round). Deferred minors: www variant built by concatenation (harmless); sec-fetch-site branch untested; receipt-cookie Secure flag carried to Task 1.3.
- Task 1.2: BASE 25b0ce1; dispatched implementer (opus) with the server-restart procedure and the check:full runbook note
- Task 1.2: implementer DONE_WITH_CONCERNS (commit 7df48e1; server on 3107 restarted on the new build). Deferred minors: production branch of check-build exercised only by the failure path until the first release build; postbuild cannot catch start-time SITE_ENV skew (a start-time check would); doc release string corrected in passing (accepted)
- Task 1.2: complete (commits 25b0ce1..7df48e1, review clean; trailer verified by controller). Deferred minors: check-build manifest read unguarded (diagnostics only); hasNoindex ignores header source; health reports "" for empty SITE_ENV (unreachable); scripts/ not covered by lint.
- Task 1.3: BASE 7df48e1; dispatched implementer (opus) with the receipt-cookie Secure-flag ruling folded in
- Task 1.3: implementer DONE_WITH_CONCERNS (commit 568fcc6, 16/16 tests). Concerns: brief tests 2/3 delete TRUST_PROXY_HEADERS without restore; cookie test cannot isolate the forwarded-proto branch because SITE_URL defaults to https; SITE_URL fallback sets Secure under plain-http next dev (receipt cookie dropped locally); process tier counts rejected requests
- Task 1.3: complete (commits 7df48e1..568fcc6, review clean; trailer verified). Ruling: process tier counts requests later rejected by the address tier (one address can exhaust 120/min) — accepted residual per brief ordering; edge rules are the primary control — cost if wrong: a single abusive client can 429 an instance for a minute. Deferred minors carried to Task 4.7 (same test file): normalise the three limiter tests to the save/restore env pattern; add a comment that the shared `valid` fixture has a 3-per-test email budget. Other minors: 5000-key cap unreachable (defence in depth); Secure cookie under plain-http non-loopback dev; redundant runbook sentence.
- Task 1.4: BASE 568fcc6; dispatched implementer (opus)
- Task 1.4: implementer DONE (commit 2ee5a79; check green, build ok, interactions 8/8 on restarted 3107). Deferred minors: abort routes to a no-op setState; response.ok not checked (falls to false); one extra health request per contact view
- Task 1.4: complete (commits 568fcc6..2ee5a79, review clean; trailer verified)
- Task 1.5: BASE 2ee5a79; dispatched implementer (opus)
- Task 1.5: implementer DONE (commit 6467e83; 7/7 targeted, 45/45 full browser suite, check + build green; 3107 restarted on the new build)
- Task 1.5: complete (commits 2ee5a79..6467e83, review clean; trailer verified). Deferred minors: spec should also assert HSTS does not contain `preload`; no CSP report-to; runbook Verification list omits the new spec; HSTS unconditional on preview hosts; `'unsafe-inline'` ceiling documented.
- Phase 1 gate: Ruling — accepted as passed on the implementer's separate runs (npm run check green, SITE_ENV=preview build with check-build ok, full Playwright 45/45 on the restarted 3107 server) instead of the single `check:full` command, which would have re-run the same three stages — cost if wrong: none.
- Task 2.1: BASE 6467e83; dispatched implementer (opus)
- Task 2.1: implementer DONE_WITH_CONCERNS (commit 14b9eb1; all pages static except /thank-you and API; 33/33 specs). Ruling: the brief's `next/script strategy="beforeInteractive"` is not parser-blocking in the App Router (measured swap ~90 ms after first paint) — replace it with a raw inline `<script dangerouslySetInnerHTML>` as the first child of `<body>` and add an HTML-order assertion to the before-hydration test; addressed before review — cost if wrong: none beyond a still-inline script the CSP already permits. Deferred minors: prerender floor of 60 gives little headroom; no-JS visitors always light (documented trade-off).
- Task 2.1: pre-review fix landed (commit 2b10b24; swap measured 174 ms vs first paint 228 ms under a delayed bootstrap; 34/34 specs). Implementer notes the order assertion would not catch a revert to next/script (suggests matching the literal script tag)
- Task 2.1: review Approved with 1 Important (controller-specified order assertion does not pin parser-blocking). Ruling: tighten to the literal '<script id="theme-init">' match; fold in the two trivial minors (single request, explicit header presence). Deferred minors: prerender floor headroom; unguarded manifest read (pre-existing style); 'unsafe-inline' now load-bearing for theming (carry to any CSP-hardening task)
- Task 2.1: fix round 1/5 (3 findings addressed per implementer, pending re-review; commits 2b10b24..94e7ab6; site.spec 23/23)
- Task 2.1: complete (commits 6467e83..94e7ab6, review clean after 1 fix round; trailers verified)
- Task 2.2: BASE 94e7ab6; dispatched implementer (opus)
- Task 2.2: implementer DONE (commit 3932618; 18/18 unit; all 29 redirects and 10 gone patterns verified by curl on restarted 3107). Note: pre-existing redirect /insights/grow-with-sap → rise-vs-grow while a grow-with-sap entry exists in content/insights.ts without a body (unpublished) — consistent; deferred minor: 410 status has no automated test
- Task 2.2: complete (commits 94e7ab6..3932618, review clean; trailer verified). Deferred minors (brief-mandated): destination test checks dynamic directory not slug; gone/redirect disjointness one-directional; noindex meta redundant on 410; force-dynamic + 24h cache on the 410 route.
- Ruling: the Task 2.2 reviewer's out-of-scope note that the sitemap advertises /insights/grow-with-sap/ is a false positive — `content/insights.ts` filters entries without a body in `insight-bodies.ts`, grow-with-sap has none, and the built sitemap.xml.body does not contain it; no action — cost if wrong: none (verified against the built artefact).
- Task 2.3+2.4 (batched): BASE 3932618; dispatched implementer (opus)
- Task 2.3+2.4: implementer DONE_WITH_CONCERNS (commits f18c242, ee7a40e; 19/19 unit; both builds check-build ok; 3107 on preview). Deferred minors: Host assertion only in the production branch; check-build does not pin the sitemap host against SITE_URL
- Task 2.3+2.4: complete (commits 3932618..ee7a40e, review clean; trailers verified). Phase 2 gate: check green, preview build check-build ok; full browser suite running (phase2-gate-playwright.log)
- Phase 2 gate: PASSED — npm run check green, SITE_ENV=preview build check-build ok, full Playwright 46/46 (11.0 min) against the restarted 3107 preview build.
- Task 3.1+3.4 (batched): BASE ee7a40e; dispatched implementer (opus)
- Task 3.1+3.4: implementer DONE_WITH_CONCERNS after a rate-limit resume (commits 0961699, 0f97fce; 21/21 unit; build ok; head tags verified on restarted 3107). Deferred minors: /404/ canonical points at a non-route (noindex, inert); Next adds its own noindex meta on not-found so two robots metas render
- Task 3.1+3.4: complete (commits ee7a40e..0f97fce, review clean; trailers verified)
- Task 3.2: BASE 0f97fce; dispatched implementer (opus)
- Task 3.2: implementer DONE (commit a743437; 23/23 unit; 11 descriptions 130-155 chars, 8 summaries 133-155)
- Task 3.2: complete (commits 0f97fce..a743437, review clean; trailer verified by reviewer). Deferred minors for the final fix wave: aptransco summary says the client "ran" the AMS engagement (scenario: engaged In2IT EBS for it) — restore the attribution; vahdam summary has "with … with"; waycool summary borrows "S/4HANA" from featuredSolution (on-page, acceptable); two values sit exactly at 155.
- Task 3.3: BASE a743437; dispatched implementer (opus)
- Task 3.3: implementer DONE_WITH_CONCERNS (commit 872f474; 24/24 unit; 62 sitemap entries dated). Ruling: three insight publication years are not stated on the scraped pages; the archive-widget inference (2023-07-17, 2025-02-25, 2023-10-12) is NOT applied because scraped/MASTER.md:775 records those index dates as corrupted by a bulk import — owner decision item (D7: confirm insight publication dates) — cost if wrong: three articles carry no date until the owner confirms. Deferred minors: grow-with-sap placeholder date (unpublished); contentUpdated is hand-maintained (release checklist).
- Task 3.3: complete (commits a743437..872f474, review clean; trailer verified). Deferred minor: test imports appended mid-file (brief said append).
- Task 3.5: BASE 872f474; dispatched implementer (opus)
- Task 3.5: implementer DONE_WITH_CONCERNS (commit be72623; 26/26 unit; build ok; 36 static inner routes carry one BreadcrumbList; Chrome used as rasteriser). Ruling: an intermediate crumb for a non-page (`/legal/` → 404) is a defect — emit intermediate crumbs only for paths present in the nav label map (all real intermediates are nav entries), always emit the final crumb; extract the item builder into lib/json-ld.ts and unit-test it; addressed before review — cost if wrong: a breadcrumb trail shorter by one non-navigable level. Deferred minors: nine-address Organization block repeated on every page (~1.9 kB); schema semantics not validated externally.
- Task 3.5: pre-review fix landed (commit 27b7aac; 27/27 unit; all 40 crumb URLs return 200). Deferred minor: isKnownPage equates page with nav entry
- Task 3.5: review Needs fixes — 2 Important: (1) /ecc-prism/ crumb "Ecc Prism" via slug fallback (plan-mandated; Ruling: pass name="ECC Prism™" on that page — cost if wrong: none); (2) labels map overwritten by hash-child hrefs, correct only because footer collection runs last (Ruling: skip hrefs containing '#', first write wins, add label tests for /advisory/, /digital-data-ai/, /delivery-excellence/). Fix round also takes minors 3 (path-equality assertion), 4 (default isPage = isKnownPage), 7 (drop unused labelFor export). Deferred minors: addressCountry names not ISO codes; publisher @id cross-block reference; footer label variants win for /case-studies/ and /partners/; thank-you crumb placement; opaque PNG with no padding; duplicate import lines; telephone optional chain.
- Task 3.5: fix round 1/5 (5 findings addressed per implementer, pending re-review; commits 27b7aac..8887dad; 28/28 unit). Ruling: first-write-wins makes primary-nav labels win over footer phrasings for four crumbs (About, Client stories, Partners, SuccessFactors / HXM) — accepted, the header label is what visitors see — cost if wrong: crumb text differs from those pages' titles.
- Task 3.5: complete (commits 872f474..8887dad, review clean after 1 fix round; trailers verified)
- Task 3.6: BASE 8887dad; dispatched implementer (opus) with the Chrome rasteriser substitution
- Task 3.6: implementer DONE (commit b2a12f1; check:full exit 0 with 46/46 browser tests = Phase 3 gate evidence). Deferred minors: /favicon.ico 404s (add app/favicon.ico later); icon fill #15213D vs source #121E3B (brief value)
- Task 3.6: complete (commits 8887dad..b2a12f1, review clean; trailer verified)
- Phase 3 gate: PASSED — implementer of 3.6 ran `SITE_ENV=preview npm run check:full` (exit 0, 46/46 browser) on the final Phase-3 build.
- Task 4.1+4.2+4.6 (batched): BASE b2a12f1; dispatched implementer (opus)
- Task 4.1+4.2+4.6: implementer DONE_WITH_CONCERNS (commits 28946cc, 7e0f869, 679174d; 30/30 unit; bracket rules present in built CSS; transcript spec 6/6 — the brief's '7' was my miscount). Ruling: footer text at 52%/58% white over navy computes to about 7:1, above AA; the Phase 4 gate axe sweep is the check — cost if wrong: a footer line under 4.5:1 caught by axe
- Task 4.1+4.2+4.6: review Needs fixes — Critical: undefined-class test regex escaping (\.${hook}\b collapsed to any-char + backspace); fix round 1 also takes the minor: line-height exclusion should be \d?xl
- Task 4.1+4.2+4.6: fix round 1/5 (2 findings addressed per implementer, pending re-review; commits 679174d..8279a79; 30/30 unit). Note for later implementers: heredoc-written regexes lose backslashes — verify bytes
- Task 4.1+4.2+4.6: complete (commits b2a12f1..8279a79, review clean after 1 fix round; trailers verified)
- Task 4.3: BASE 8279a79; dispatched implementer (opus)
- Task 4.3: implementer DONE_WITH_CONCERNS after a network-error resume (commit 454ad66; 38/38 across three specs incl. axe). Ruling: the brief's img count selector also matched the SAP badge image; the tightened  locator with count 2 (plus an after-Next assertion) is accepted as the brief's intent — cost if wrong: none. Deferred minor: heading level-2  order dependence
- Task 4.3: implementer DONE_WITH_CONCERNS after a network-error resume (commit 454ad66; 38/38 across three specs incl. axe). Ruling: the brief's img-count selector also matched the SAP badge image; the tightened img[srcset] locator with count 2 (plus an after-Next assertion) is accepted as the brief's intent — cost if wrong: none. Deferred minor: heading level-2 .first() order dependence.
- Task 4.3: review Approved with 1 Important (plan-mandated): two-image mount window removes the crossfade on forward transitions. Ruling: widen the mounted set to previous, active and next (three of four images); the image-count test expects 3 — cost if wrong: one extra hero image request on load (the brief's aim was avoiding all four).
- Task 4.3: fix round 1/5 (1 finding addressed per implementer, pending re-review; commits 454ad66..4c96844; transcript 7/7)
- Task 4.3: complete (commits 8279a79..4c96844, review clean after 1 fix round; trailers verified). Deferred minor: pagination jumps (non-adjacent) still pop in without a fade.
- Ruling: batch Tasks 4.4 and 4.5 into one dispatch (two single-component UI changes with their own browser assertions) — cost if wrong: a review that rejects one part rejects both.
- Task 4.4+4.5 (batched): BASE 4c96844; dispatched implementer (opus)
- Task 4.4+4.5: implementer DONE (commits 405d05c, b9f51dd; navigation 7/7, interactions 9/9, site 23/23). Deferred minors: no rootMargin on the footer observer; focus drops to body if the closed button is focused when it hides; no hide/show transition
- Task 4.4+4.5: review Approved with 1 Important (plan-mandated): hiding the quick-contact root while focused drops keyboard focus. Ruling: track focus-within on the root and suppress hiding while focused (hidden only when nearFooter, closed and unfocused); fix round 1 — cost if wrong: the button stays visible over the footer for keyboard users only. Deferred minor: xl is now a shared breakpoint contract for nav, toggle and region control
- Task 4.4+4.5: fix round 1/5 (1 finding addressed per implementer, pending re-review; commits b9f51dd..bbdd22e; interactions 10/10, repeat-each 5 stable)
- Task 4.4+4.5: complete (commits 4c96844..bbdd22e, review clean after 1 fix round; trailers verified)
- Task 4.7: BASE bbdd22e; dispatched implementer (opus) carrying the Task 1.3 deferred test-hygiene items
- Task 4.7: implementer DONE (commit 630631d; 31/31 unit; region spec 1/1; footer now nine data-derived cities). Deferred: deliveryCities (offices.ts) and salesOffices (about page) still hardcode cities incl. "Johannesburg" vs office data "Germiston" — owner ruling item under M7 figures.
- Task 4.7: complete (commits bbdd22e..630631d, review clean; trailer verified). Deferred minor: stale JSX comment REGIONAL EMAILS in contact page. Phase 4 gate suite running (phase4-gate-playwright.log)
- Phase 4 gate: PASSED — check green, preview build check-build ok, full Playwright 49/49 (12.1 min) on the restarted 3107 build.
- Task 5.1: BASE 630631d; dispatched implementer (opus)
- Task 5.1: implementer DONE_WITH_CONCERNS (commit bc625d6; 11 files deleted incl. orphaned content/signature-offerings.ts; 32/32 unit). Concern: unused-module test matches basenames anywhere (a slug string masked converged-intelligence.tsx) — reviewer to weigh; worklog line indentation
- Task 5.1: review Approved with 1 Important (plan-mandated): unused-module test matches quoted basenames outside import specifiers. Ruling: anchor to from/require/import( specifiers and add synthetic-string assertions; fix round 1 — cost if wrong: a stricter regex could miss an exotic import form and flag a live module (visible as a false failure, not a silent pass). Deferred minor: worklog line indentation
- Task 5.1: fix round 1/5 (1 finding addressed per implementer, pending re-review; commits bc625d6..fa4c966; 32/32 unit). Deferred minor: synthetic fixture strings sit inside a scanned file and could self-vouch for those two basenames
- Task 5.1: complete (commits 630631d..fa4c966, review clean after 1 fix round; trailers verified)
- Task 5.2: BASE fa4c966; dispatched implementer (opus)
- Task 5.2: implementer DONE_WITH_CONCERNS (commits 808c154 test tweak for bracket spacing, 14051dd formatting of 108 files; check incl. format:check green; build ok; transcript 7/7). Ruling: two commits accepted (keeps the formatting commit pure) — cost if wrong: none. Deferred minor: source-grepping tests are formatting-coupled
- Task 5.2: complete (commits fa4c966..14051dd, review clean; trailers verified). Deferred minors: bracketSpacing default coupling with source-grepping tests; extract-client-marks.py not formatted (no Python parser).
- Task 5.3: BASE 14051dd; dispatched implementer (opus)
- Task 5.3: implementer DONE (commit 5a97c79; no Commons vector for NALCO India — rasterised via Chrome to 11 KB PNG; check 32/32; client-coverage 2/2). Deferred: jindal-steel-power.svg 101 KB and sharechat.jpg 160 KB remain heavy; NALCO trademark unchanged (LEGAL_REVIEW)
- Task 5.3: complete (commits 14051dd..5a97c79, review clean; trailer verified). Deferred minor: manifest row sits under the 8 Sep additions table heading.
- ALL TASKS COMPLETE. Phase 5 gate suite running (phase5-gate-playwright.log). Final whole-branch review dispatched (fable) on package review-branch-0a1a094..5a97c79.diff with final-review-triage-list.md.
- Phase 5 gate: PASSED — full Playwright 49/49 (12.2 min) on the final tree (5a97c79); check green; build check-build ok. Awaiting final whole-branch review.
- Final whole-branch review (fable): Ready to merge WITH FIXES — 0 Critical, 6 Important (runbook sentences; APTRANSCO summary attribution; limiter tier order; X-Forwarded-For appending-ingress warning; HSTS includeSubDomains precondition; 410/308 HTTP assertions), minors triaged. Ruling: one fix wave takes the six Importants plus four trivial minors (app/favicon.ico, .gitattributes binary types, reuseExistingServer !CI, stale JSX comment, vahdam "with … with"); the prerender-floor rewrite, build-time SITE_ENV stamp, Prettier/ESLint glob widening and TRUSTED_PROXY_HOPS stay deferred as follow-ups — cost if wrong: those four remain open on the branch. Fix-wave BASE 5a97c79.
- Final fix wave: implementer DONE_WITH_CONCERNS (commits 5a97c79..e787d6d, 7 commits; check 32/32; build ok; security-headers+interactions 12/12; favicon 200). Concerns: *.ico added to .gitattributes; ICO is a PNG wrapper (IE≤10 shows nothing); !CI reuse untested without a remote; HSTS precondition documented not closed
- Final fix wave: re-review clean (all 6 Important + 4 minors ADDRESSED; two cosmetic minors noted: runbook lacks the CI reuse carve-out; ICO bBitCount 32 vs 24-bit PNG). Final browser suite on e787d6d running.
- Final browser suite on e787d6d:   50 passed (10.1m). All tasks complete; final review clean after one fix wave. Ledger and final review copied into docs/superpowers/plans/ before workspace deletion.
