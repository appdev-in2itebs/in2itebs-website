# SDD ledger — plan: docs/superpowers/plans/2026-09-10-premium-glass-restyle.md

Spec: docs/superpowers/specs/2026-09-10-premium-glass-restyle-design.md (read; binding authority).
Branch: restyle/2026-09-10-premium-glass from main 17277ca. Plan committed at 83d1d30.
Preview server: 127.0.0.1:3107 (controller-owned, SITE_ENV=preview, pid 1960 at start); Cloudflare tunnel https://unified-symphony-downloaded-commitments.trycloudflare.com (pid 37236).

## Preflight rulings

- Ruling: no git worktree; work happens on the branch in the main tree — the repo has no worktree setup, the user runs parallel sessions here, and the remediation used the same arrangement — costs collision with another session's edits (implementers re-read before editing).
- Ruling: implementers run on Opus for every task, reviewers on Sonnet for mechanical diffs and Opus for hero/motion/header/sweep diffs, final review on Fable — the user asked for "Fable to orchestrate and Opus to write the code" — costs more per task than the cheapest tier.
- Ruling: Tasks 6 and 7 are dispatched as one batch (the chunk-budget assertion in Task 6 only passes once Task 7 wires the loader into the page) — costs one larger review surface.
- Ruling: Tasks 13 and 14 stay separate dispatches (35 files of mechanical mapping is too long for one agent turn budget) — costs a second reviewer seat.
- Ruling: plan amended before Task 1 (commit follows): Task 1 tailwind test accepts unquoted keys (Prettier leaves `gold`/`glass` unquoted); Task 6 scene keeps its own elapsed-time accumulator instead of three's Clock (Clock.start() resets to zero and the sculpture would jump on resume); Task 7 caption bar uses `glass-elevated` (normal text over a photo); Task 10 partner tiles keep `bg-logo-surface` (dark logos on dark glass would vanish in the dark theme) — costs nothing if right; if wrong the reviewers will say so.

## Preflight scan

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 ↔ T2 (globals.css) | T1 tokens (`--color-gold*`, `--glass-*`, `--gradient-*`) consumed by T2 utilities | consistent; T2 introduces `--glass-line-alpha`/`--glass-top-alpha`/`--glass-inset-alpha` with fallbacks |
| T1 ↔ T2/T12 (globals.css edits) | additive edits to different blocks | no conflict |
| T1 test ↔ T7 tailwind snippet | test regex required quoted keys; Prettier unquotes `gold`, `glass` | **defect** → fixed (optional quotes) |
| T2 ↔ T3 (reveal CSS vs markup) | `.reveal`, `.stagger`, `--reveal-delay`, `data-reveal="in"` | consistent |
| T2 test (reduced block) | `block()` takes the first `@media (prefers-reduced-motion: reduce)` — the top-level one where `.reveal` is added | consistent |
| T3 ↔ T3 tests | regexes `Math\.min\(index, 8\) \* 70`, `dataset\.motionOwner = "controls"`, `delete document\.documentElement\.dataset\.motionOwner`, `dataset\.reveal = "in"` vs given code | consistent |
| T3 ↔ T6 (motion flags) | Hero3D observes `data-motion-paused`; MotionControls owns it on the homepage | consistent |
| T4 ↔ T9/T10 (`Card`, `Button`) | props unchanged | consistent |
| T5 header ↔ site/transcript specs | ids, aria, xl breakpoint, region combobox retained by instruction | consistent |
| T6 ↔ T7 (loader ↔ hero, chunk budget) | budget needs the loader imported by a page | **ordering** → batched as one dispatch |
| T6 scene `Clock` | `Clock.start()` resets elapsed → jump on resume, contradicts spec §4.3 "does not jump" | **defect** → fixed (own accumulator) |
| T6 ↔ T7 spec | `data-hero-3d` states and reasons identical in loader, scene `onState`, spec | consistent |
| T7 caption bar | `glass` (0.62) under normal text over a photo vs spec §3.3 "panels holding normal text use elevated alpha" | **defect** → fixed (`glass-elevated`) |
| T9 mapping ↔ T14 regex | every class the regex bans has a mapping row | consistent (`font-serif` rows present in T9/T10/T13) |
| T10 partner tiles | dropping `bg-logo-surface` puts dark partner marks on dark glass in the dark theme | **defect** → fixed (keep it) |
| T12 stats `dl` | `metric-item::before` animated line over glass cards | compatible (no overflow clip on `.glass-card`) |
| T13 ↔ T14 (sweep + regression test) | T14's test fails until T13's files are clean; T14 lands the test after finishing its own files | consistent by ordering |
| Opacity modifiers across tasks | all `/N` values multiples of 5 or bracketed | consistent with the foundations test |
| T15 docs | no code | n/a |

## Task log

- Task 1: dispatched (BASE 44cb7f8, implementer Opus)
- Task 1: implementer DONE at 06340eb (check green, contrast spec 2/2); concerns noted: gold-display on bg-subtle light = 3.528 (thin margin), Prettier trims trailing zeros (test regex adjusted with comment); review dispatched (Sonnet) on review-44cb7f8..06340eb.diff
- Task 1: review (Sonnet) — spec ✅; Important (plan-mandated): light gold-display on bg-subtle = 3.528 vs 3.5 floor, fragile. Minor (deferred): dark --gradient-heading-on-brand duplicates --gradient-heading verbatim (plan-mandated).
- Ruling: lower light --color-gold-display from 58% to 57% lightness (≈3.69:1 on bg-subtle) and amend spec §3.1 to match — the AA floor is a binding constraint and the spec value was an argument, not a requirement; 0.8% headroom is not a guarantee — costs a slightly darker gradient tail on light headings.
- Task 1: fix round 1/5 dispatched (resume implementer)
- Task 1: fix round 1/5 — fix at d6fe116 (gold-display 57%, ratio on bg-subtle 3.678; spec §3.1 amended); scoped re-review dispatched (Sonnet) on review-06340eb..d6fe116.diff
- Task 1: fix round 1/5 (1 addressed, 0 open; commits 06340eb..d6fe116)
- Task 1: complete (commits 44cb7f8..d6fe116, review clean)
- Task 2: dispatched (BASE d6fe116, implementer Opus)
- Task 2: implementer DONE at dca115d (check green, 39/39 unit); controller probe: Tailwind CLI output keeps `main h1, main h2` and `.theme-on-brand`-keyed rules without any component usage; class-keyed utilities (glass, reveal, nav-link, text-gold, heading-plain) appear once used — expected on-demand behaviour, no action; review dispatched (Sonnet) on review-d6fe116..dca115d.diff
- Task 2: review (Sonnet) — spec ✅, quality Approved. Minor (deferred): glass utilities repeat border/backdrop declarations (plan-mandated verbatim CSS); no test pins the numeric glass values; reveal-hiding test matches first occurrence only.
- Task 2: complete (commits d6fe116..dca115d, review clean)
- Task 3: dispatched (BASE dca115d, implementer Opus)
- Task 3: implementer DONE_WITH_CONCERNS at b96a3c5 (check green 41 unit; full browser suite 50/50). Concerns (observations): tests are source greps; anchor jumps can leave skipped reveals hidden until they intersect; client-mounted .reveal after the effect is never observed — carry into section-task dispatches: keep Reveal/Stagger in server markup. Review dispatched (Opus) on review-dca115d..b96a3c5.diff
- Task 3: review (Opus) — spec ✅ (⚠️ prerender floor: controller eyeballs one build route table before the final review). Important (plan-mandated): ordering assertion passes when MotionObserver is deleted; `delay` prop inert at 23 call sites. Minor (deferred): read/write interleave in the two measurement loops; dead `--stagger-step` custom property duplicates the 70ms step; client-mounted .reveal never observed (comment or MutationObserver); `child.type === StaggerItem` degrades across an RSC boundary; safety pass samples once.
- Ruling: honour `Reveal delay` as `--reveal-delay: delay*1000ms` (call sites pass seconds) rather than deleting the prop — 23 authored intents beat a typecheck cleanup — costs nothing if wrong beyond a few hundred ms of stagger. Ruling: add one Playwright assertion in interactions.spec.ts that a below-the-fold reveal on /about/ becomes visible on scroll — the task exists to produce that behaviour and no committed test proved it — costs one more spec runtime.
- Task 3: fix round 1/5 dispatched (resume implementer)
- Task 3: fix round 1/5 — fix at c5973d8 (presence-then-order assertion; Reveal delay honoured; scroll-reveal browser test on /about/); scoped re-review dispatched (Sonnet) on review-b96a3c5..c5973d8.diff. ⚠️ prerender floor: resolved — check-build asserts the floor and passed in the fix-round build.
- Task 3: re-review agent killed by the session rate limit (429) before reporting; relaunched (Sonnet) on the same package.
- Task 3: fix round 1/5 (2 addressed, 0 open; commits b96a3c5..c5973d8)
- Task 3: complete (commits dca115d..c5973d8, review clean)
- Task 4: dispatched (BASE c5973d8, implementer Opus)
- Task 4: implementer DONE at 14433a1 (check green, build ok). Consumer-side notes to carry into Tasks 13/14: on Card/glass-card elements drop legacy border/ring classes instead of mapping them (delivery-excellence:138 `border border-navy/5` flattens the glass edge); not-found.tsx:33 overrides the ghost Button colours. Review dispatched (Sonnet) on review-c5973d8..14433a1.diff
- Task 4: review (Sonnet) — spec ✅, quality Approved. Minor (deferred): `Card tone="navy"` now ignores `bezel` (plan-mandated); note for consumer call sites.
- Task 4: complete (commits c5973d8..14433a1, review clean)
- Task 5: dispatched (BASE 14433a1, implementer Opus)
- Task 5: implementer DONE_WITH_CONCERNS at fd5c911 (check green; site+transcript+interactions 41/41). Deviation: footer social `.glass-pill` got `bg-on-brand/10` because `.theme-on-brand` does not remap `--color-glass` (near-white pill on navy, 1.43:1).
- Ruling: accept the local footer fix, and add a token-layer remap in Task 12 (globals.css step): `.theme-on-brand { --color-glass: var(--color-bg-brand); --glass-line-alpha: 0.12; --glass-top-alpha: 0.22; }` so every glass surface inside an on-brand scope tints to the brand ground — the spec §3.1 remap covers gold only, an oversight — costs a retint of any on-brand glass added before Task 12 (none besides the footer).
- Controller check on the shimmer worry: a `::after` with z-index -1 inside `isolation: isolate` paints above the element background and below its content (CSS painting order), so `.btn-shimmer` on opaque `bg-action` is visible; no action. Minor (deferred): header transition list omits border-color, so the gold border snaps on scroll.
- Task 5: review dispatched (Opus) on review-14433a1..fd5c911.diff
- Controller visual check at fd5c911 (/about/ top + footer, light and dark): gradient headings, gold eyebrow, gold nav underline, glass utility pill, scrolled frosted header, gold footer labels and glass social pills all render as designed. One artefact to re-check in the final visual gate: in the scrolled-to-bottom capture the fixed header appears offset from the top by ~55px (light) / ~18px (dark), possibly a smooth-scroll capture timing effect.
- Task 5: review (Opus) — spec ✅, quality Approved. ⚠️ mega-menu contrast over dark content: controller ran axe on the open #desktop-nav-0 overlaying the industry tiles, both themes: 0 violations, 17 incomplete (axe cannot compute through backdrop-filter) — screenshots inspected, see next line. ⚠️ hover contrast: resolved — hover colours are the text-safe gold roles covered by the contrast pairs. Minor (deferred): footer `bg-on-brand/10` deviation undocumented in code (superseded by the Task 12 on-brand glass remap); "Talk to us" utility pill hit area 36px (plan-mandated, above the 24px floor); the scrolled alpha override also lowers the no-backdrop-filter fallback (0.96→0.90); mobile close button hover class differs from the desktop chevron. Report concern about btn-shimmer confirmed incorrect by the reviewer too.
- Task 5: complete (commits 14433a1..fd5c911, review clean)
- Tasks 6+7: dispatched as one batch (BASE fd5c911, implementer Opus)
- Ruling (Task 5 ⚠️ resolution): mega-menu over dark imagery — axe clean in both themes and text legible on inspection, but the light-theme panel at alpha 0.78 lets busy photography bleed through; raise the panel to `[--glass-alpha-elevated:0.92]` in the final fix wave (header.tsx, one class) rather than opening a fix round now — costs one more item in the fix wave; if wrong, the menu stays readable either way.
- Tasks 6+7: implementer DONE at 7037b55 (T6) and 9b0edce (T7); check green 43 unit; chunk bd904a5c…js 204447 bytes lazy-only; hero+transcript+interactions 23/23 (WebGL present, pause path exercised). Deviations: check-build adapted to its `failures` array; `typeof window.requestIdleCallback === "function"` (the `in` form narrows to never); pause test scrolls back to top after each click (control is below the fold, offscreen gate held it paused). Concern: 7037b55 alone does not build (chunk absent until T7) — accepted, inherent to the batch. Review dispatched (Opus) on review-fd5c911..9b0edce.diff
- Controller visual check at 9b0edce (/ hero, light and dark, state active in both): sculpture, gold rings, glass frame and tab bar render as designed. Note for the final visual gate: in the light theme the knot edge passes under the hero body copy (x≈540-760px at 1440); legible, but consider camera.position.x = -5 to push the sculpture further right if the final review agrees.
- Tasks 6+7: review (Opus) — spec ❌ on two plan-mandated points; Important (plan-mandated): (1) loader sets `pending` on every resize because the `mounted` guard reads the initial closure — attribute lies over a running scene; (2) `blocker()` acquires a WebGL context on every un-debounced resize; (3) check-build scans `.next/static/chunks` non-recursively, blind to 51 route chunks under app/ and pages/. Minor: (4) no forceContextLoss before dispose; (5) sync() never re-asserts active; (6) initial sync() beats the IntersectionObserver so a below-fold hero renders one frame then freezes; (7) renderer construction failure leaves `pending` forever; (8) dark spec asserts nothing theme-specific; (9) settled() two-round-trip race; (10) small gold text over the canvas is not measurable by computed-background contrast checks. ⚠️ build line from reviewed HEAD: accepted — the implementer built 9b0edce for the specs; ⚠️ interactions contrast coverage: resolved — that spec asserts behaviour, not contrast; contrast pairs live in theme-contrast.spec.ts on tokens.
- Ruling: fix (1)(2)(3) and fold in (4)(5)(6)(7) as one state-machine correction — they are the same mechanism and each is a few lines; (8)(9)(10) deferred to the final review — costs a larger fix diff for the re-review to read.
- Tasks 6+7: fix round 1/5 dispatched (resume implementer)
- Tasks 6+7: fix round 1/5 — fix at 34889d4 (mountedRef, cached+debounced probe, recursive chunk scan 17→68 files, forceContextLoss, self-healing sync, offscreen-first, unavailable path); implementer removed the now-unused eslint-disable directive (ruled fine: an unused directive fails --max-warnings 0). Deferred: cached probe canvas keeps one context for the document lifetime. Scoped re-review dispatched (Sonnet) on review-9b0edce..34889d4.diff
- Tasks 6+7: fix round 1/5 (7 addressed, 0 open; commits 9b0edce..34889d4). Minor (deferred): renderer-failure `unavailable` has no permanent latch (retry only on a later debounced resize).
- Tasks 6+7: complete (commits fd5c911..34889d4, review clean)
- Task 8: dispatched (BASE 34889d4, implementer Opus)
- Task 8: implementer DONE_WITH_CONCERNS at 6f54b93 (check green, build ok). Addition accepted: PageHero Container gets `relative` so the hero-ambient layer paints beneath the copy (same pattern as service-hero). Carry into Task 14: the three photo consumers (about, careers, industries/[slug]) pass `aspect-[4/3] rounded-xl3` media into the 5/4 glass frame — make them `h-full w-full rounded-[calc(var(--radius-feature)-0.5rem)] object-cover` wrappers with no own aspect ratio. CtaSection top hairline removed per brief (deferred note). Review dispatched (Sonnet) on review-34889d4..6f54b93.diff
- Task 8: review (Sonnet) — spec ✅, quality Approved. Minor (deferred to the final fix wave): PageHero Container should be `relative z-10` like service-hero; page-hero doc comment still says "serif headline". Photo-consumer frame mismatch carried to Task 14.
- Task 8: complete (commits 34889d4..6f54b93, review clean)
- Task 9: dispatched (BASE 6f54b93, implementer Opus)
- Task 9: implementer DONE_WITH_CONCERNS at 63a8490 (grep clean, check green, build ok). Concerns: (1) mapping row `border-l-4 border-gold` colours all four edges and overrides the glass hover border — plan defect; Ruling: pillar cards use `border-l-4 border-l-gold` (fold into fix round 1 with any review findings) — costs nothing if wrong beyond a hairline colour. (2) dropped the shells own transition classes on glass-card (accepted, matches Card precedent). (3) pillars grid now three separate glass cards — eyeball in the final visual gate. (4) stale accent doc comment rewritten. Review dispatched (Sonnet) on review-6f54b93..63a8490.diff
- Task 9: review (Sonnet) — spec ✅ (one plan-mandated ❌), quality Approved. Important (plan-mandated): `border-l-4 border-gold` on pillar cards paints all edges. Minor (deferred): transition-utility removal not enumerated in the mapping (justified). ⚠️ pillars visual rhythm: check in the final visual gate.
- Task 9: fix round 1/5 dispatched (resume implementer)
- Task 9: fix round 1/5 — fix at 7adf671 (border-l-gold; built CSS verified); scoped re-review dispatched (Haiku) on review-63a8490..7adf671.diff
- Task 9: fix round 1/5 (1 addressed, 0 open; commits 63a8490..7adf671)
- Task 9: complete (commits 6f54b93..7adf671, review clean)
- Task 10: dispatched (BASE 7adf671, implementer Opus)
- Task 10: implementer DONE at 6ddd7be (grep clean, check green, build ok, client-coverage+interactions 13/13). Notes: `glass` on the comparison-table scroller adds a containing block (no fixed descendants today); toolchain/full-ecosystem hover classes dropped per brief. Review dispatched (Sonnet) on review-7adf671..6ddd7be.diff
- Task 10: review (Sonnet) — spec ✅, quality Approved. Minor (deferred): comparison-table JSDoc says "on white" (stale).
- Task 10: complete (commits 7adf671..6ddd7be, review clean)
- Task 11: dispatched (BASE 6ddd7be, implementer Opus)
- Task 11: implementer DONE_WITH_CONCERNS at b1cdfa3 (check green, build ok; transcript+interactions 17/18 — the one failure "transcript homepage at 320px" is pre-existing from Task 10: platform-practice-links.tsx tiles overflow at 320px, proven by stashing and rebuilding at 6ddd7be). Concerns: selected option card `ring-1 ring-gold` drops the glass shadow; `.glass-card:focus-within` outranks `border-gold` until focus leaves.
- Ruling: the 320px overflow is a Task 10 defect found after completion; fix it in Task 12 (add `min-w-0` to each practice link and let the label wrap; keep the arrow `shrink-0`) since Task 12 reruns transcript.spec.ts — costs Task 12 one extra file; if wrong, the spec fails again and says so. Ruling: selected option card gets `shadow-glass` back alongside the gold ring (fold into the Task 11 fix round if one opens, else the final fix wave).
- Task 11: review dispatched (Sonnet) on review-6ddd7be..b1cdfa3.diff
- Task 11: review (Sonnet) — spec ✅, Important (plan-mandated): selected option card `border-gold ring-1 ring-gold` loses the glass shadow (ring replaces box-shadow) and `.glass-card:hover/:focus-within` override its border and ring. Minor (deferred): divider and check-dot hover mappings were judgment calls.
- Ruling: extend scope into globals.css with a `.glass-card-selected` component utility whose selector list covers rest, :hover and :focus-within (border gold, box-shadow = glass shadow + 1px gold ring), used as `checked && "glass-card-selected"` in the chooser; add the hook to the design-system utilities test — a reusable selected state beats a per-file arbitrary shadow — costs one more utility to maintain.
- Task 11: fix round 1/5 dispatched (resume implementer)
- Task 11: fix round 1/5 — fix at c1b438b (.glass-card-selected in both the base and hover-media blocks, plus a `.theme-dark .glass-card-selected:hover` override the implementer added because `.theme-dark .glass-card:hover` is (0,2,1) — accepted); six computed readings pass. Scoped re-review dispatched (Sonnet) on review-b1cdfa3..c1b438b.diff
- Task 11: fix round 1/5 (1 addressed, 0 open; commits b1cdfa3..c1b438b). Evidence gap closed by the controller: built CSS grep (see next line).
- Task 11: built CSS a80734103d64430f.css contains `glass-card-selected` ×7 including `.theme-dark .glass-card-selected:hover` ×1 — gap closed.
- Task 11: complete (commits 6ddd7be..c1b438b, review clean)
- Task 12: dispatched (BASE c1b438b, implementer Opus) with two ruled extras: `.theme-on-brand` glass remap in globals.css; `min-w-0` fix in platform-practice-links.tsx for the 320px overflow.
- Task 12: implementer DONE_WITH_CONCERNS at 48c53e0 (grep clean, check green, build ok; transcript+interactions+client-coverage+hero 25/25 incl. 320px; all-routes+site+theme-contrast 28/28). Extra B needed `[overflow-wrap:anywhere]` in addition to `min-w-0` (accepted). Minor (deferred to the final fix wave, ruled): practice tiles grid → `grid-cols-1 min-[480px]:grid-cols-2 sm:grid-cols-3 lg:grid-cols-6` so 320-479px phones get one column instead of mid-word breaks (keep the overflow-wrap safety net); remove the leftover `bg-surface-subtle` on the featured stage card wrapper. Review dispatched (Sonnet) on review-c1b438b..48c53e0.diff
- Controller visual check at 48c53e0 (/ full page, light and dark, reduced motion): stats cards, gradient headings, gold eyebrows, glass rows/stage cards/industry tiles/trust strip/CTA and footer all coherent. Minor (deferred, ruled): in the dark theme the static logo wall (expanded or reduced-motion state) shows a cross pattern where rounded light tiles meet over the noir 1px gaps — in the final fix wave drop `rounded-surface` on marquee tiles while `data-expanded="true"` or under reduced motion (CSS in globals: `.client-ribbon[data-expanded="true"] .client-ribbon-list > div { border-radius: 0 }` plus the reduced-motion block).
- Task 12: review (Sonnet) — table rows and extra B ✅; Important: extra A inert on the footer pills (`bg-on-brand/10` utility beats the glass background; `.theme-dark .glass-pill` direct rule shadows the inherited alpha vars in dark). Minor: featured stage wrapper `bg-surface-subtle` bleeds through the glass card (already ruled for the fix wave); report rationale on `text-foreground` inertness was wrong (harmless); decorative blue `bg-action` dots and `stage-featured-tint` still blue (out of scope).
- Ruling: keep `--color-glass: var(--color-bg-brand)` on `.theme-on-brand` (inherited), move the two alpha vars into a direct-target rule `.theme-on-brand .glass, .theme-on-brand .glass-elevated, .theme-on-brand .glass-card, .theme-on-brand .glass-pill` placed after the `.theme-dark` alpha rule (same specificity, later source wins in both themes), and remove the footer social pill `bg-on-brand/10` workaround so the remap is exercised; fold in dropping `bg-surface-subtle` from the featured stage wrapper — costs a footer retint that axe on the footer must confirm.
- Task 12: fix round 1/5 dispatched (resume implementer)
- Task 12: fix round 1/5 — fix at a25585f (alpha vars moved to a direct-target on-brand rule after the dark rule; footer `bg-on-brand/10` removed; featured stage wrapper bg dropped); probe: pill background = bg-brand at 0.78 in both themes, alphas 0.12/0.22 in dark too, axe footer clean, specs 20/20. Scoped re-review dispatched (Sonnet) on review-48c53e0..a25585f.diff
- Task 12: fix round 1/5 (1 addressed, 0 open; commits 48c53e0..a25585f)
- Task 12: complete (commits c1b438b..a25585f, review clean)
- Task 13: dispatched (BASE a25585f, implementer Opus)
- Task 13: implementer DONE at 4ae8004 (20 files; grep CLEAN; check green; build ok, routes static; all-routes 3/3, zero axe violations). Judgment calls accepted: sand aliases swept; on-brand numerals use text-gold-on-brand (the numeral gradient starts navy-on-navy in on-brand scopes); SubPracticeCard rebuilt on pillar-cards; accent border widths kept; focus rings mapped to ring-focus. Ruling for Task 14: extend the regression regex to `(?:text|bg|border|rule)-sand`, map `rule-sand`→`rule-gold` and `border-sand/40`→`border-gold-soft/40`; leave the `.rule-sand` CSS definition in place. Review dispatched (Sonnet) on review-a25585f..4ae8004.diff
- Task 13: review (Sonnet) — content-safe sweep ✅; Important (plan-mandated conflict): proof-band h2s in 10 on-brand pages mapped `text-white`→`text-on-brand` instead of dropping the colour, silencing the on-brand heading gradient. Minor: stale "Light-blue accent edge" comment in digital-data-ai; unnecessary `theme-on-brand` on a 48px medallion span in delivery-excellence; thin (≈4.7:1) margin on small gold uppercase text in why-in2it-ebs (deferred).
- Ruling: the controller ruling (drop h1/h2 colour classes) wins over the brief table row for headings — the on-brand gradient rule exists for exactly these headings; fold in the two trivial minors (comment, span) — costs a fix round on 10 files.
- Task 13: fix round 1/5 dispatched (resume implementer)
- Task 13: fix round 1/5 — fix at 750e90c (heading colour dropped on 10 proof-band h2s + what-we-do h2; comment and medallion minors); gradient readings verified in both themes; theme-contrast+site 25/25. Implementer concern: `SectionHeading` passes `text-foreground`/`text-on-brand` to the `Headline` h2, silencing the gradient on every SectionHeading site-wide.
- Ruling: fix `SectionHeading` in Task 14 (extra item): for levels 1–2 pass no colour class (gradient rules paint them, on-brand included via the scope); level 3 keeps `text-foreground`/`text-on-brand` — costs Task 14 one more file; all-routes runs there anyway.
- Task 13: scoped re-review dispatched (Sonnet) on review-4ae8004..750e90c.diff
- Task 13: fix round 1/5 (1 addressed, 0 open; commits 4ae8004..750e90c)
- Task 13: complete (commits a25585f..750e90c, review clean)
- Task 14: dispatched (BASE 750e90c, implementer Opus) with ruled extras: SectionHeading level 1-2 colour drop; regex extended to sand aliases incl. rule-sand; photo-frame consumers (about, careers, industries/[slug]); h1/h2 colour classes dropped across app/ incl. page.tsx.
- Task 14: session lost after dispatch — the implementer never reported or committed; working tree at 750e90c carries its uncommitted work (15 files: 13 pages, typography.tsx SectionHeading fix, foundations.test.ts regression test with the sand/rule-sand regex). Controller check: foundations 21/21 passing (regression test green); no h1/h2 under app/ carries a colour class. Fresh implementer (Sonnet) dispatched to verify the tree against the brief and ruled extras, run check/build/all-routes, write the report and commit.
- Ruling (Task 14 extra): the h1/h2 colour-drop ruling extends to the two component headings still carrying `text-foreground` — `components/sections/cta-section.tsx:32` and `components/sections/home-partners.tsx:17` — because the `main h1, main h2` gradient rule is meant to paint every heading and a flat colour class silences it exactly as the SectionHeading defect did; costs two one-line edits — if wrong, those two headings show the gradient rather than flat navy, which is the restyle's intended look anyway.
- Ruling (Task 15 pre-flight): the plan and spec name `web/WORKLOG.md`, which does not exist; the only worklog is the git-tracked root `WORKLOG.md`. Task 15 appends its dated entry to the root file (commit path `../WORKLOG.md` from web/) and creates no second worklog — one log beats two; if wrong, the entry moves one directory, nothing else changes.
- Task 14: implementer (Sonnet, recovery) DONE at 5f1b372 (17 files; foundations 21/21; npm run check 44/44; build ok 72/72 static routes; all-routes 3/3 both themes, zero axe violations). Added the two ruled component heading drops; dropped redundant `ring-1 ring-white/10` on two navy Cards (noted for review). Review dispatched (Sonnet) on review-750e90c..5f1b372.diff
- Task 14: review (Sonnet) — all brief files and extras 1–5 verified (three legal pages accounted for; regex, SectionHeading, photo frames, heading colour drop incl. the two components); Important: `insights/[slug]/page.tsx:57` decorative motif `border-navy`→`border-gold-soft` instead of the table's `border-border-subtle`. Minor (deferred): `ring-1 ring-white/10` dropped from three `Card tone="navy"` usages (case-studies/[slug] ×2, partners ×1) — outside the mapping table, verified redundant with Card's own `border-gold-on-brand/20` + `shadow-glass`.
- Task 14: parked — `insights/[slug]/page.tsx:57` `border-navy`→`border-gold-soft` — Ruling: the code stands. The element is the same aria-hidden rotated-square blueprint motif that `page-hero.tsx:30` has rendered as `border-gold-soft` since Task 8; the table's `border-navy` row targets element borders (card edges, rings), not decorative fills, and the motif sits under `opacity-[0.05]` with no contrast role. Mapping it to `border-border-subtle` would make the article header the one inner page whose motif is grey — costs one class on one decorative element if wrong.
- Task 14: complete (commits 750e90c..5f1b372, 1 parked)
- Task 15: dispatched (BASE 5f1b372, implementer Sonnet) with ruled extra: worklog entry goes in the root `WORKLOG.md`.
- Deviation note (controller, 2026-09-11): the Task 14 recovery implementer and the Task 15 implementer ran on Sonnet, not Opus as ruled at line 10 — Task 14's sweep code was written by the original Opus implementer (Sonnet verified it, ran the gates and added two one-line class drops), and Task 15 writes documentation only. The final fix wave writes code and runs on Opus; the final whole-branch review runs on Fable, as ruled.
- Task 15: implementer (Sonnet) DONE_WITH_CONCERNS at 4e88987 (DESIGN.md, runbook, root WORKLOG.md; check 44/44; build ok, chunk 204447 bytes lazy-only; full Playwright 54/56 — 2 deterministic failures in transcript.spec.ts mega-menu axe scans on /what-we-do/ and /contact/: colour-contrast violations on unrevealed .reveal elements). transcript.spec.ts last passed at 48c53e0 (Task 12); Tasks 13–14 ran all-routes only — regression window a25585f..5f1b372 or a latent timing dependency. Routed to the final review/fix wave. Review dispatched (Sonnet) on review-5f1b372..4e88987.diff
- Ruling: the final whole-branch review is dispatched on 17277ca..4e88987 before the Task 15 docs review closes — every code task is complete and Task 15 can only change documentation; any docs fix commits are folded into the fix wave's scoped re-review range — costs a re-read of a small docs diff if the docs review opens a fix round.
- Final review: dispatched (Fable) on review-17277ca..4e88987.diff with deferred-and-rulings.md; the transcript mega-menu failure is stated as a known failing gate.
- Diagnosis: dispatched (Opus, read-only on the main tree, worktree bisect across a25585f, 4ae8004, 750e90c, 5f1b372) for the transcript.spec.ts mega-menu axe failures; report to transcript-failure-diagnosis.md; feeds the fix wave.
- Task 15: review (Sonnet) — all brief steps and the three ruled extras ✅; every spot-checked value matches code/spec (tokens, contrast facts, radii, gradient rule, gating, chunk budget, regression test, commit count). Important (brief-text conflict): DESIGN.md:50 and WORKLOG.md:7 list `prefers-reduced-motion` among the hero's pause conditions; spec §4.2/§4.3 and `hero-3d-loader.tsx` treat it as a mount blocker (state `unavailable`, reason `reduced-motion`) and `hero.spec.ts:42-48` asserts exactly that. Minor (deferred): worklog `a2ba46d..5f1b372` double-dot notation reads as 23 commits though the stated 24 is right for main..5f1b372; DESIGN.md source-of-truth rows use "—" placeholders in the Background column.
- Ruling: the spec wins over the brief's "pause rules (page control, reduced motion, hidden tab, offscreen)" wording — both docs say the hero pauses under the page control, a hidden tab and offscreen state, and that reduced motion (like a sub-1024px viewport, data saver or no WebGL) never mounts the scene at all; fold in the worklog range notation (`main..5f1b372`) — costs a docs-only fix round.
- Task 15: fix round 1/5 dispatched (resume implementer; docs only, no build or browser run — the diagnosis agent owns port 3107)
- Task 15: fix round 1/5 — fix at e5cc01a (DESIGN.md + WORKLOG.md reduced-motion wording; worklog range notation); check green; scoped re-review dispatched (Sonnet) on review-4e88987..e5cc01a.diff
- Task 15: fix round 1/5 (2 addressed, 0 open; commits 4e88987..e5cc01a). Minor (deferred to the fix wave): DESIGN.md:50 first sentence's mount-condition list no longer complete on its own (reduced motion only in the following sentence); WORKLOG.md:7 dropped the "page visible" mount condition.
- Task 15: complete (commits 5f1b372..e5cc01a, review clean)
- Final review (Fable) on 17277ca..4e88987 — Not ready to merge. Critical: (1) demo-form fields lost the 3:1 boundary (glass border on glass), (2) the failing gate is a reveal-system defect (base .reveal transitions the hide, so below-fold content fades 1 to 0 at hydration and axe scores fractional opacity). Important: (3) Card bezel ring silences the glass shadow on about 20 cards; (4) main h2 gradient reaches three sub-large h2s; (5) background-tab load leaves the hero unavailable (no visibilitychange); (6) Accent words inherit the gradient — unruled; (7) text-on-glass contrast unverifiable by axe — add composite pairs; (8) backdrop-filter surface count; (9) ruled ledger items still open. Minors 10–19 (not-found hover, resize blank when paused, probe context, read-write interleave, scrolled alpha vs @supports fallback, regex text-white, doc nits, on-brand gradient dup, dark hero spec assertion, hover conventions). Triage: all controller rulings confirmed; insights motif parking confirmed; mega-menu 0.92 reclassified as necessary.
- Ruling: accent words inside gradient h1/h2 inherit the gradient (no blue word) — the spec's direction is deep-gold and champagne accents and the gradient's gold tail is that accent; DESIGN.md and a code comment record it — costs one CSS line (text-fill-color currentColor on .accent) if the owner wants the blue word back. SURFACE TO THE USER.
- Ruling: Card bezel is fixed with a .glass-card-bezel utility (shadow list plus inset 1px gold-soft at 40% across rest, hover and focus-within, with the dark hover override) rather than outline, which would clobber focus-visible on link cards — costs one more utility to maintain.
- Ruling: composite text-on-glass pairs cover only the combinations in use (mega-menu panel over canvas and brand; glass-card text over canvas and subtle; on-brand glass) rather than a cross-product — a pair nothing uses would fail for nothing; mega-menu alpha starts at 0.92 and rises to the lowest passing value if the pair fails.
- Ruling: backdrop-filter is dropped from .glass-pill and .glass-gold only (invisible at chip alpha); .glass-card keeps blur at all widths; the performance sample asserts a relative bound (at most 3x a glass-light control route) so it survives slow machines — costs a test that could still prove flaky on CI.
- Ruling: the reveal fix (B2) is written into the brief as the reviewer's mechanism, subject to the diagnosis agent's bisect (section C); the fix wave waits for that report before dispatch — costs the wait.
- Ruling: minors 10–16, 18, 19 fold into the wave (each is a few lines and the wave runs the full suite anyway); 17 (hover conventions) stays deferred — a many-file convention change with no correctness stake.
- Diagnosis (Opus, terminated by the session rate limit before writing its report; evidence recovered from its scratch logs): the two mega-menu tests pass at dca115d and fail from b96a3c5 (Task 3 reveal system) and at a25585f, so not a Task 13/14 regression; at HEAD they pass alone (2/2) and fail under the full suite (54/56 twice). Snapshots at a25585f: on what-we-do 12 of 17 below-fold .reveal elements were at partial opacity (0.66-0.83) when axe ran, 46 colour-contrast violations at 1.31-1.37; on contact all had reached opacity 0, 0 violations, 38 incompletes. Confirms the final review's B2 mechanism (fade-out race on the base .reveal transition). Leftover bisect worktree removed; main checkout clean at e5cc01a; nothing on port 3107.
- Final fix wave: dispatched (BASE e5cc01a, implementer Opus) on fix-wave-brief.md (A1-A6 ruled items, B1-B10 final-review findings with rulings, C diagnosis, D1-D9 minors; D10 deferred). Report to fix-wave-report.md.
- Final fix wave: implementer (Opus) DONE at da6447a — 10 commits 6d1cfde..da6447a (reveal transition on the in state; field border; .glass-card-bezel; three h2 opt-outs + .heading-plain specificity; visibilitychange recovery; composite text-on-glass pairs; chips without backdrop-filter + scroll frame-timing sample; ledger minors A2-A5/B9/D1/D4/D5/D8/D9; regex white prefixes; docs). check 44/44; build ok (204447-byte lazy chunk); FULL Playwright 57/57 in one run (both mega-menu tests pass; WebGL pause test ran); frozen content clean. Concerns: (1) A1 panel alpha as a utility also lowers the no-backdrop-filter fallback 0.96 to 0.92; (2) SAP hub at 375px sits at about 1.9x of the 3x scroll-timing bound; (3) .theme-dark .glass-card-bezel:hover redundant today.
- Ruling (fix-wave concern 1): parked — the mega-menu fallback at 0.92 is still an opaque-enough panel and the composite pair that now guards the panel is computed at that alpha, so contrast holds without backdrop-filter; moving it into the base layer like D5 is consistency, not correctness — costs two lines later if wanted.
- Ruling (concerns 2, 3): parked — the 375px SAP hub sample is inside the bound with margin and the redundant dark hover selector is harmless and mirrors .glass-card-selected as briefed.
- Final fix wave: scoped re-review dispatched (Opus) on review-e5cc01a..da6447a.diff
- Controller visual gate at da6447a (preview server on 3107, light and dark, 1440x1000): hero knot clear of the body copy in both themes (camera x -5); dark logo wall expanded shows square tiles, no cross pattern; light mega-menu at 0.92 over the industries hero is legible with only a faint ghost of the large heading behind it (glass by design, composite pairs guard it); contact form fields have a visible boundary in both themes; /about/ scrolled to the bottom has the header at y=0 in both themes — the earlier 'offset' was a fixed-header artefact of fullPage captures; the three pillar cards sit in an even rhythm. Visual gate: pass. Captures in the session scratchpad visual-gate/.
- Final fix wave: scoped re-review (Opus) — A1-A6, B1-B10, D1-D4, D6-D9 ADDRESSED with file:line evidence (reveal hide has no transition; transcript.spec.ts byte-identical; composite arithmetic is glass-over-background; no pair relaxed; bezel utility carries the glass shadow; heading-plain specificity confirmed; D10 confirmed not done). D5 NOT ADDRESSED: header[data-scrolled=true] at (0,1,1) still outranks the @supports fallback at (0,1,0) — Tailwind v3 emits no cascade layers, so the base-layer move is a no-op. New Minor breakage: comments in globals.css:286-290 and header.tsx:69-70 assert a false cascade fact; B4's wider .heading-plain (0,1,1) makes the companion text-foreground inert so the insights article h2s inherit fg-secondary from their muted parent (AA holds, tonal separation lost); performance.spec.ts:65 hardcodes the 3107 URL instead of baseURL. Out of scope: .glass-pill still listed in the @supports fallback block (harmless).
- Ruling (breaker, no second wave): D5 parked — real, deferred; affects only browsers without backdrop-filter, where the scrolled header panel resolves to 0.90 instead of 0.96 and stays legible; the working form is a scoped @supports rule: @supports not (backdrop-filter: blur(1px)) { header[data-scrolled="true"] { --glass-alpha-elevated: 0.96 } } plus deleting the two false comments — costs a three-line follow-up commit.
- Ruling (breaker): B4 side-effect parked — real, deferred, cosmetic; the two insight-article h2 levels paint fg-secondary (a tested 4.5 pair) instead of fg-primary; fix is to give that h2 its own colour with higher specificity or to drop color from the .heading-plain rule and keep only the fill/background reset — costs one line; nothing downstream builds on it.
- Ruling (breaker): performance.spec.ts hardcoded URL and the .glass-pill fallback entry parked — harmless today; fold into the same follow-up commit.
- Final fix wave: complete (commits e5cc01a..da6447a, 4 parked). Final review clean apart from the parked follow-ups; visual gate passed; full suite 57/57 at da6447a.
